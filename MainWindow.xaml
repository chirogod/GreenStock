<Window x:Class="GreenStock.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GreenStock"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp" 
        xmlns:viewmodels="clr-namespace:GreenStock.ViewModels"
        mc:Ignorable="d"
        Title="GreenStock"
        WindowState="Maximized"
        Height="1080" Width="1920">


    <Window.DataContext>
        <viewmodels:MainWindowViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/> 
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Border Grid.Column="0" 
                Background="{StaticResource SidebarBackground}" 
                BorderBrush="#EEEEEE" 
                BorderThickness="0, 0, 1, 0">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>    <RowDefinition Height="*"/>      <RowDefinition Height="Auto"/>    </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="15, 15, 15, 20">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🌿" FontSize="24" Foreground="{StaticResource AccentColor}" VerticalAlignment="Center" Margin="0, 0, 8, 0"/>
                        <StackPanel>
                            <TextBlock Text="GreenStock" FontWeight="Bold" FontSize="18" Foreground="{StaticResource SidebarTextColor}"/>
                            <TextBlock Text="Gestión Inteligente" FontSize="12" Foreground="{StaticResource InactiveTextColor}"/>
                        </StackPanel>
                    </StackPanel>
                    <Separator Margin="0, 15, 0, 0" Background="#EEEEEE" Height="1"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Orientation="Vertical">

                    <Button Style="{StaticResource SidebarButtonStyle}"
                            Command="{Binding NavigateToDashboardCommand}">
                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Margin="0, 0, 8, 0" Icon="House" Width="16" Height="16"/>
                            <TextBlock Text="Dashboard"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource SidebarButtonStyle}"
                            Command="{Binding NavigateToConfigCommand}">
                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Margin="0, 0, 8, 0" Icon="Calculator" Width="16" Height="16"/>
                            <TextBlock Text="Caja"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource SidebarButtonStyle}"
                            Command="{Binding NavigateToConfigCommand}">
                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Margin="0, 0, 8, 0" Icon="CartShopping" Width="16" Height="16"/>
                            <TextBlock Text="Ventas"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource SidebarButtonStyle}"
                            Command="{Binding NavigateToProductCommand}">
                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Margin="0, 0, 8, 0" Icon="Box" Width="16" Height="16" />
                            <TextBlock Text="Productos"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource SidebarButtonStyle}"
                            Command="{Binding NavigateToConfigCommand}">
                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Margin="0, 0, 8, 0" Icon="ShoppingBag" Width="16" Height="16"/>
                            <TextBlock Text="Compras"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource SidebarButtonStyle}"
                            Command="{Binding NavigateToSupplierCommand}">
                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Margin="0, 0, 8, 0" Icon="Truck" Width="16" Height="16"/>
                            <TextBlock Text="Proveedores"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource SidebarButtonStyle}"
                            Command="{Binding NavigateToCatalogoCommand}">
                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Margin="0, 0, 8, 0" Icon="Tag" Width="16" Height="16"/>
                            <TextBlock Text="Catalogo"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource SidebarButtonStyle}"
                            Command="{Binding NavigateToClientCommand}">
                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Margin="0, 0, 8, 0" Icon="Users" Width="16" Height="16"/>
                            <TextBlock Text="Clientes"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource SidebarButtonStyle}"
                            Command="{Binding NavigateToUserCommand}">
                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Margin="0, 0, 8, 0" Icon="UserCheck" Width="16" Height="16"/>
                            <TextBlock Text="Usuarios"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource SidebarButtonStyle}"
                            Command="{Binding NavigateToConfigCommand}">
                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Margin="0, 0, 8, 0" Icon="FileArrowUp" Width="16" Height="16" />
                            <TextBlock Text="Reportes"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource SidebarButtonStyle}"
                            Command="{Binding NavigateToConfigCommand}">
                        <StackPanel Orientation="Horizontal">
                            <fa:IconImage Margin="0, 0, 8, 0" Icon="Gear" Width="16" Height="16"/>
                            <TextBlock Text="Configuracion"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="15, 15, 15, 15">
                    <TextBlock Text="Dietética San Martín" FontSize="12" Foreground="{StaticResource InactiveTextColor}"/>
                    <TextBlock Text="v1.0.0" FontSize="12" FontWeight="Bold" Foreground="{StaticResource InactiveTextColor}"/>
                </StackPanel>
                
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="#F0F0F0">
            <ContentControl Content="{Binding CurrentViewModel}" />
        </Border>
    </Grid>
</Window>